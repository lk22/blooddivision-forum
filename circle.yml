machine:

  services:
    - mysql

  php:
    version: 7.0.2

  hosts:
    bloodforum.app: 127.0.0.1

dependencies:
  override:
    - sudo apt-get install gettext
    - sudo a2enmod mod_gettext
    - composer install
    - vendor/bin/phpunit 
